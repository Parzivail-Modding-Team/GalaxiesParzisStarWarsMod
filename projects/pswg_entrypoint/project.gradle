dependencies {
	// For running in the IDE
	api(project(":projects:pswg_blasters"))
	api(project(":projects:pswg_gadgets"))

	// For building a jar
	include(project(path: ":projects:pswg_core"))
	include(project(path: ":projects:pswg_blasters"))
	include(project(path: ":projects:pswg_gadgets"))
}

base.archivesName = 'pswg'
