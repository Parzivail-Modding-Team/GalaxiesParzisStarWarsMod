dependencies {
	"runtimeOnly"(project(":projects:pswg", configuration = "namedElements"))
	"runtimeOnly"(project(":projects:tarkin", configuration = "namedElements"))
	"runtimeOnly"(project(":projects:addon-test", configuration = "namedElements"))
}
